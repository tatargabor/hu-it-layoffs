## 1. Add GoatCounter script

- [x] 1.1 Insert GoatCounter `<script>` tag before `</body>` in the HTML template string in `src/visualize.py` `generate_html()` function

## 2. Verify

- [x] 2.1 Run `python -m src.visualize` and confirm `report.html` contains the GoatCounter script tag
